{
  "name": "nfl-injury-dashboard",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "generate": "python3 generate_data.py --csv data/WAS_KC_filtered.csv --out src/nflData.json",
    "predev":   "npm run generate",
    "prebuild": "npm run generate",
    "dev":      "vite",
    "build":    "vite build",
    "preview":  "vite preview"
  },
  "dependencies": {
    "react":    "^18.2.0",
    "react-dom":"^18.2.0",
    "recharts": "^2.12.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.2.0",
    "vite":                 "^5.2.0"
  }
}